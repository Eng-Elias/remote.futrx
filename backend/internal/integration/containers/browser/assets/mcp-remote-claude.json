{
  "mcpServers": {
    "browser": {
      "type": "http",
      "url": "${REMOTE_BROWSER_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${REMOTE_BROWSER_MCP_TOKEN}"
      }
    }
  }
}
